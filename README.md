# RNN-visualizer
Interactive web app to visualize how recurrent neural networks (GRU and LSTM) process information step by step. Covers sentiment analysis and encoder-decoder translation with optional attention, exposing embeddings, hidden states, gates, and softmax outputs at every timestep. Built as a teaching tool for university deep learning courses.
